spring:
  application:
    name: rdv-service
  config:
    import: optional:configserver:http://config-service:8888
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        hostname: rdv-service
        health-check-path: /rdv/actuator/health
      host: consul-server
      port: 8500

logging:
  level:
    org:
      springframework:
        web: DEBUG
  file:
    name: logs/logs.log
