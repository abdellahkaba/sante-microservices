spring:
  application:
    name: rdv-service
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        hostname: rdv-service
        health-check-path: /actuator/health
      host: consul-server
      port: 8500

server:
  port: 8082

---

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres-ms:5432/rdv-db
    username: username
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
